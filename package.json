{
    "name": "ark",
    "version": "1.0.0",
    "scripts": {
        "build": "esbuild ./src/index.ts --sourcemap --bundle --outfile=./index.js --platform=node --target=node14 --format=cjs"
    },
    "devDependencies": {
        "@types/node": "latest",
        "esbuild": "latest"
    }
}
